{
  "name": "api",
  "module": "./src/index.ts",
  "type": "module",
  "scripts": {
    "bun": "bun",
    "test": "bun test",
    "build": "bun build --target=node . --outdir ./dist --sourcemap=external "
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun": "^1.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
